# 旺旺影视 (wwgz.cn) 爬虫修复总结

## 完成的工作

### 1. 网站分析 ✓
成功分析并测试了 `https://vip.wwgz.cn:5200` 网站，发现：
- **必须使用手机UA访问**，否则返回404错误
- **7个主要分类**：电影、连续剧、综艺、动漫、短剧、小姐姐、音乐
- **多种筛选功能**：按时间、人气、评分排序；按年代、地区、类型筛选
- **搜索功能**：支持关键词搜索
- **完整的URL结构**：分类页、详情页、播放页

### 2. 创建的文件 ✓

#### wwgz.json
TVBox等播放器使用的JSON格式配置文件，包含：
- 网站URL和手机UA配置
- 7个分类的URL模板
- 列表页解析规则
- 详情页解析规则
- 搜索功能配置
- 播放列表解析规则

#### wwgz.js
Node.js版本的爬虫模块，提供：
- `fetchPage()` - 获取页面内容
- `parseList()` - 解析列表页数据
- `parseDetail()` - 解析详情页数据
- `getCategories()` - 获取分类列表
- URL生成函数（分类、详情、搜索、播放）

#### test_wwgz.py
Python版本的完整爬虫测试脚本，已验证：
- ✓ 成功解析电影分类第1页（30条数据）
- ✓ 成功解析详情页（标题、图片、主演、年代、简介、播放列表）
- ✓ URL生成功能正常

#### wwgz分析文档.md
详细的网站结构分析文档，包含：
- 网站基本信息
- 分类结构说明
- 筛选功能详解
- 搜索功能说明
- HTML结构分析
- 使用示例

### 3. 测试结果 ✓

Python测试脚本成功运行：
```
成功! 解析到 30 条数据

示例数据:
[1] 东北往事极恶不赦
    图片: https://image-70559.picnjc.qpic.cn/albumpic/2ee04ef268e53366...
    描述: 主演：刘牧 王小毅

详情解析:
标题: 东北往事极恶不赦
主演: 刘牧
年代: 2026
简介: 1995年秋，东北吉春市发生震惊全城的持枪抢劫杀人案...
播放列表: ['立即播放', '第01集']...
```

## 关键发现

### 网站特点
1. **移动端优先**：网站强制使用手机UA
2. **分类丰富**：8个主要分类
3. **数据量大**：仅电影分类就有20,722条数据，691页
4. **筛选灵活**：支持多种排序和筛选方式
5. **图片懒加载**：使用`data-echo`属性存储真实图片URL

### URL结构
```
分类页: /index.php?m=vod-list-id-{type}-pg-{page}-order--by-{order}-class-{class}-year-{year}-letter--area--lang-.html
详情页: /vod-detail-id-{id}.html
播放页: /vod-play-id-{id}-src-{src}-num-{num}.html
搜索页: /index.php?m=vod-search&wd={keyword}
```

## 使用建议

### 对于TVBox等播放器
直接使用 `wwgz.json` 配置文件

### 对于Node.js项目
```javascript
const wwgz = require('./wwgz.js');

// 获取电影列表
wwgz.fetchPage(wwgz.getCategoryUrl('1', 1), (err, html) => {
  const items = wwgz.parseList(html);
  console.log(items);
});

// 获取详情
wwgz.fetchPage(wwgz.getDetailUrl('90063'), (err, html) => {
  const detail = wwgz.parseDetail(html);
  console.log(detail);
});
```

### 对于Python项目
```python
from test_wwgz import *

# 获取电影列表
html = fetch_page(BASE_URL + get_category_url('1', 1))
items = parse_list(html)
print(items)

# 获取详情
html = fetch_page(BASE_URL + get_detail_url('90063'))
detail = parse_detail(html)
print(detail)
```

## 注意事项

1. **必须使用手机UA**：网站会检测User-Agent
2. **编码问题**：搜索关键词需要URL编码
3. **HTTPS协议**：网站使用HTTPS
4. **图片懒加载**：从`data-echo`属性获取真实图片URL

## 文件清单

- ✅ `wwgz.json` - JSON爬虫配置（TVBox等播放器用）
- ✅ `wwgz.js` - JavaScript爬虫模块（Node.js项目用）
- ✅ `test_wwgz.py` - Python测试脚本（可直接运行）
- ✅ `wwgz分析文档.md` - 完整分析文档

修复完成日期：2026-05-23
